<UserControl
    x:Class="WpfDependencyInjection.Views.Parent1Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfDependencyInjection.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:WpfDependencyInjection.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:Parent1PageViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <DockPanel Background="LightBlue">
        <TextBlock DockPanel.Dock="Top" Text="Parent1" />
        <TextBlock DockPanel.Dock="Top" Text="{Binding Index.Index, StringFormat='Index={0}'}" />
        <TextBlock DockPanel.Dock="Top" Text="{Binding Counter}" />

        <ContentControl x:Name="content" Margin="10" />
    </DockPanel>
</UserControl>
